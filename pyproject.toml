[project]
name = "kstudy"
version = "0.1.0"
description = "Gerenciador de estudos"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "zuban>=0.7.2",
]
